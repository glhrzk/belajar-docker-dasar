# untuk melihat penggunaan resource dari tiap tiap docker yang aktif kita bisa menggunakan docker container stats, stats akan terus aktif sampai kita menutupnya
# syntax, docker container stats
# example
docker container stats